/**
 * Dashboard-style pages — stat grids and panels below the header.
 */
export function OverviewLayout({ children }: { children: React.ReactNode }) {
  return <div className="space-y-6">{children}</div>;
}
