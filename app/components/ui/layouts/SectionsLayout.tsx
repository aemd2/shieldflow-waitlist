/**
 * Multi-section grid pages — Integrations and similar grouped catalogs.
 */
export function SectionsLayout({ children }: { children: React.ReactNode }) {
  return <div className="space-y-6">{children}</div>;
}
